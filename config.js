/**
 * SolarLink CRM — runtime config
 *
 * This file holds your Supabase project URL + publishable (anon) key.
 * It is intentionally gitignored — never commit it.
 *
 * The publishable key is safe to expose in the browser because every
 * table is protected by Row-Level Security policies (see db/schema.sql).
 */
window.SOLARLINK_CONFIG = {
  supabaseUrl:     'https://fsgstepnomneydpidjop.supabase.co',
  supabaseAnonKey: 'sb_publishable_AbVhJrCnTWBEore7G2uv2g_HGXda0_W'
};
